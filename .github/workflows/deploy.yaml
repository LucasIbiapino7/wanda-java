name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: [self-hosted, linux, wanda-java]

    steps:
      - name: Deploy local
        run: |
            cd /var/www/wanda/wanda-java
            git fetch origin main
            git reset --hard origin/main
            docker compose up --build -d
